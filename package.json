{
  "name": "army-control-pane",
  "private": true,
  "scripts": {
    "dev:router": "pnpm --filter @vera/router dev",
    "dev:control-plane": "pnpm --filter @vera/control-plane dev",
    "deploy:router": "pnpm --filter @vera/router deploy",
    "deploy:control-plane": "pnpm --filter @vera/control-plane deploy",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp", "workerd"]
  }
}
