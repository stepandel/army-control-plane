{
  "name": "@army/control-plane",
  "private": true,
  "scripts": {
    "dev": "bash dev.sh",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "dbmate --no-dump-schema up",
    "db:rollback": "dbmate --no-dump-schema rollback",
    "db:status": "dbmate status",
    "db:new": "dbmate new"
  },
  "dependencies": {
    "@army/shared": "workspace:*",
    "hono": "^4.7.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250228.0",
    "dbmate": "^2.24.0"
  }
}
