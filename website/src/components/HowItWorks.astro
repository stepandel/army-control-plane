---
const stages = [
  {
    name: "Research",
    label: "#research",
    description:
      "Vera investigates the problem space: reads the codebase, searches documentation, analyzes competitor approaches, and gathers the context needed to make informed decisions.",
    screenshot: "/screenshot-research.png",
    screenshotAlt: "Vera researching a codebase in Linear with the #research label",
  },
  {
    name: "Prep & Plan",
    label: "#prep",
    description:
      "Before writing a single line of code, Vera sizes the work, validates scope, identifies entry points, and creates a concrete implementation plan — just like a senior engineer would.",
    screenshot: "/screenshot-prep.png",
    screenshotAlt: "Vera creating an execution plan with sub-issues in Linear with the #prep label",
  },
  {
    name: "Execute",
    label: "#execute",
    description:
      "Vera implements the solution: writes production code, follows existing patterns and conventions, adds tests, and opens a pull request with a clear description of what changed and why.",
    screenshot: "/screenshot-execute.png",
    screenshotAlt: "Vera completing a task lifecycle — from prep to PR opened in Linear",
  },
  {
    name: "Triage",
    label: "#triage",
    description:
      "When bugs surface or tickets pile up, Vera investigates root causes, assesses impact, and either fixes issues directly or provides detailed analysis so your team can act fast.",
    screenshot: "/screenshot-triage.png",
    screenshotAlt: "Vera triaging and fixing a bug with results shown in Linear and Slack",
  },
];
---

<section id="how-it-works" class="how-it-works" aria-labelledby="hiw-heading">
  <div class="section-inner">
    <div class="section-header">
      <h2 id="hiw-heading">
        <span class="section-wordmark">
          <img
            src="/linear-logo-dark.svg"
            alt=""
            class="section-logo"
            width="40"
            height="40"
          />
          <span class="section-wordmark-text">Linear</span>
        </span>
      </h2>
    </div>
    <p class="section-intro">
      Vera ships with prebuilt Linear labels. Add one to a ticket and Vera
      switches modes instantly, from deep research to prep, execution, and
      triage.
    </p>

    <div class="book-wrapper"><div class="book" id="book">
      {
        stages.map((stage, i) => (
          <div
            class:list={["book-page", { active: i === 0 }]}
            data-index={i}
          >
            <div class="page-spine">
              <span class="spine-label"><span class="linear-dot"></span>{stage.label}</span>
            </div>
            <div class="page-content">
              <div class="page-text">
                <span class="linear-label"><span class="linear-dot"></span>{stage.label}</span>
                <h3 class="page-name">{stage.name}</h3>
                <p class="page-desc">{stage.description}</p>
              </div>
              <div class="page-screenshot">
                <img src={stage.screenshot} alt={stage.screenshotAlt} loading="lazy" />
              </div>
            </div>
          </div>
        ))
      }
    </div></div>
  </div>
</section>

<script>
  const book = document.getElementById("book");
  const pages = document.querySelectorAll<HTMLDivElement>(".book-page");
  const count = pages.length;
  let timer: ReturnType<typeof setTimeout> | null = null;

  function activate(index: number) {
    pages.forEach((p, i) => {
      if (i === index) {
        p.classList.add("active");
      } else {
        p.classList.remove("active");
      }
    });
  }

  pages.forEach((page, i) => {
    page.addEventListener("mouseenter", () => {
      if (page.classList.contains("active")) return;
      if (timer) clearTimeout(timer);
      timer = setTimeout(() => activate(i), 80);
    });

    page.addEventListener("click", () => {
      if (timer) clearTimeout(timer);
      activate(i);
    });
  });

  book?.addEventListener("mouseleave", () => {
    if (timer) clearTimeout(timer);
  });
</script>

<style>
  .how-it-works {
    padding: 5rem 1.5rem;
    border-top: 1px solid rgba(13, 40, 24, 0.15);
  }

  .section-inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .book-wrapper {
    max-width: 100vw;
    margin-left: calc(-50vw + 50%);
    margin-right: calc(-50vw + 50%);
  }

  h2 {
    display: flex;
    justify-content: center;
    font-size: clamp(1.75rem, 3.5vw, 2.5rem);
    font-weight: 700;
    text-align: center;
    margin: 0;
    letter-spacing: -0.02em;
  }

  .section-header {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-bottom: 1rem;
  }

  .section-logo {
    display: block;
    width: auto;
    height: 1em;
    flex-shrink: 0;
  }

  .section-wordmark {
    --linear-pill-scale: 1;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.45em;
    padding: 0.3em 0.62em;
    font-size: calc(2.1rem * var(--linear-pill-scale));
    line-height: 1;
    border: 1px solid rgba(13, 40, 24, 0.14);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.3);
    box-shadow: 0 1px 0 rgba(13, 40, 24, 0.04);
  }

  .section-wordmark-text {
    font-size: 1em;
  }

  .section-intro {
    text-align: center;
    color: var(--color-text-muted);
    font-size: 1.125rem;
    max-width: 640px;
    margin: 0 auto 3rem;
    line-height: 1.6;
  }

  .book {
    display: flex;
    height: 480px;
    border-top: 1px solid rgba(13, 40, 24, 0.15);
    border-bottom: 1px solid rgba(13, 40, 24, 0.15);
    overflow: hidden;
  }

  .book-page {
    position: relative;
    flex: 0 0 60px;
    overflow: hidden;
    cursor: pointer;
    border-right: 1px solid rgba(13, 40, 24, 0.15);
    transition: flex 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .book-page:last-child {
    border-right: none;
  }

  .book-page.active {
    flex: 1 0 0;
    cursor: default;
  }

  /* Spine — always rendered, fades out when active */
  .page-spine {
    position: absolute;
    inset: 0;
    width: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    user-select: none;
    opacity: 1;
    transition: opacity 0.3s ease;
  }

  .spine-label {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.15rem 0.55rem;
    border: 1px solid rgba(13, 40, 24, 0.15);
    border-radius: 100px;
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--color-text);
    white-space: nowrap;
    transform: rotate(90deg);
  }

  .book-page.active .page-spine {
    opacity: 0;
    pointer-events: none;
  }

  /* Content — always rendered, fades in when active */
  .page-content {
    display: grid;
    grid-template-columns: 1fr 1.4fr;
    gap: 2rem;
    align-items: center;
    padding: 2rem;
    height: 100%;
    opacity: 0;
    transition: opacity 0.4s ease 0.15s;
  }

  .book-page.active .page-content {
    opacity: 1;
  }

  .page-text {
    display: flex;
    flex-direction: column;
  }

  .linear-label {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.15rem 0.55rem;
    border: 1px solid rgba(13, 40, 24, 0.15);
    border-radius: 100px;
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--color-text);
    width: fit-content;
    margin-bottom: 0.75rem;
  }

  .linear-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #34D399;
    flex-shrink: 0;
  }

  .page-name {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
    color: var(--color-text);
  }

  .page-desc {
    font-size: 0.95rem;
    color: var(--color-text-muted);
    line-height: 1.6;
  }

  .page-screenshot {
    border-radius: 8px;
    overflow: hidden;
    border: 1px solid rgba(13, 40, 24, 0.15);
    max-height: 420px;
  }

  .page-screenshot img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: top left;
    display: block;
  }

  @media (max-width: 768px) {
    .book {
      flex-direction: column;
      height: auto;
    }

    .book-page {
      flex: 0 0 48px;
      border-right: none;
      border-bottom: 1px solid rgba(13, 40, 24, 0.15);
      transition: flex 0.5s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .book-page:last-child {
      border-bottom: none;
    }

    .book-page.active {
      flex: 1 0 auto;
    }

    .page-spine {
      width: 100%;
    }

    .spine-label {
      transform: none;
    }

    .page-content {
      display: flex;
      flex-direction: column;
      padding: 1.5rem;
    }

    .page-screenshot {
      max-height: 300px;
    }
  }
</style>
